# i have about 3 programim=ng techniques to do it 
# 1st just use the int library like decimal = int(binary, 2)
# 2nd is through looping/recursion technique and by applying the basic math
# now 3rd bitwise operations
# 4rth is just store it in a variable in 0bxxxxxxxxxxx formate when we try to access it the python will autometically convert it in dsecimal 




#def calc_decimal(binaryNumber):
#     initial_list = []
#     reversed_list = []
#     second_last_list = []
#     final_list = []
#     iteration = 0
#     value_counter = 0
#     last_value_counter = 0
#     #because of input function  takes inputs are in string fata type so its an iteratable formate lets iterate and create a list if these numvers 
#     for char in binaryNumber:
#         initial_list.append(char)

#     reversed_list = list(reversed(initial_list))# by default reversed() method returns a memory location not list so we must hAVE TO use it inside list() function itr returns a reversed list
    
#     for item in reversed_list:
#         # get the all elements one by one and use them after converting to int by dec=fault im storig in string formate
        
#         for iteration2 in range(iteration):
#             value = int(item) * 2
#             second_last_list.append(str(value))
#         for second_last_list_item in second_last_list:
#             value_counter = int(second_last_list_item) + value_counter
        
#         final_list.append(str(value_counter))
#         value_counter = 0
#         iteration = 0
#         # final_list.append(value)
#         iteration = iteration + 1

#     for final_list_item in final_list:
#         last_value_counter = int(final_list_item) + last_value_counter

#     return last_value_counter



def calc_decimal(binaryNumber):
    numBer = binaryNumber
    return numBer













# # 

# binary_number = input("Enter the binary ====>>>> ")
# print(f"The Decimal Of that provided binary is {calc_decimal(binary_number)}")

